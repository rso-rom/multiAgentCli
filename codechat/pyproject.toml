[project]
name = "codechat"
version = "0.1.0"
description = "Claude Code style CLI with backend adapters"
requires-python = ">=3.10"
dependencies = [
  "click>=8.1",
  "requests>=2.31",
  "pygments>=2.17",
  "rich>=13.0"
]
